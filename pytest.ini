[pytest]
pythonpath = .
asyncio_mode = auto
markers =
    e2e: сквозной прогон контура на живом тестнете (E2E_TESTNET=1 + тестнет-ключи)
filterwarnings =
    ignore:.*non-checked-in connection.*:sqlalchemy.exc.SAWarning
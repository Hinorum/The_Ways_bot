[pytest]
pythonpath = .
asyncio_mode = auto
filterwarnings =
    ignore:.*non-checked-in connection.*:sqlalchemy.exc.SAWarning
markers =
    slow: тяжёлые тесты (PIL-рендеры, полные сбросы); исключаются `-m "not slow"`

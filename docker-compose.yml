services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: way
      POSTGRES_PASSWORD: way
      POSTGRES_DB: the_way
    ports:
      - "5432:5432"
    volumes:
      - way_pg:/var/lib/postgresql/data

volumes:
  way_pg:

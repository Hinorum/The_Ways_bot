# Soak-отчёт: живой кругооборот казны на тестнете

Дата: 23.09.2026 (UTC). Контур: The Way's · бот + казначей на TON testnet.

## Резюме

Полный денежный цикл протонян на **живом тестнете реальными монетами**: ставка →
подтверждение → закрытие дня → жеребьёвка → приз/рейк → отправка → сверка зеркала.
Исходящие выплаты ушли через новый **HTTP-канал отправки** (OST-блокер: ADNL/TCP до
лайтсерверов режется сетью машины), казначей задеплоен той же транзакцией.
Зеркало сходится «в ноль», очередь пуста. **666 passed, 1 skipped**, ruff чист.

## Контур

| Параметр | Значение |
|---|---|
| Сеть | testnet (global id −3) |
| Казначей | v5r1, `0:7375481e…b257ef7e` (свежий, генерирован под прогон) |
| Кошелёк игрока | v5r1 `0:1c3e8381…9f92aa` (E2E_PLAYER, id 5974934737) |
| Получатель рейка | `0:f5b1039c…b4b42` (OWNER_WALLET_ADDRESS) |
| БД | `data/e2e_testnet.db` (свежая, посеяна: Player + Round day 1 OPEN, majority, money_mode, 3 карты) |
| Ключи индексаторов | TON_API_KEY и TONCENTER_API_KEY заполнены (в `.env`) |
| Источник лайтсерверов | `LITESERVER_CONFIG_URL=https://ton.org/testnet-global.config.json` |

## Хронология денег (все суммы нанотоны, хеши hex)

| Событие | Сумма | Транзакция | memo |
|---|---|---|---|
| Ставка игрока (вход) | +500 000 000 | `a53b806e…6f46fdab` | (депозит) |
| Деплой казны (HTTP, денежно-нейтральный) | 0 | `722279eb…e7fd5c` | — |
| Приз победителю | −475 000 000 | `6131ac91…312e` | `way:1:prize#1` |
| Рейк владельцу | −2 500 000 | `13592dc4…3974` | `way:1:rake#2` |

- Приз ушёл с `seqno=1`, рейк с `seqno=2` (батч-счётчик не дал коллизии).
- Газ/комиссии из цепочки суммарно ≈ 0.0018 Gram (деплой + 2 исходящих).
- **Баланс казны 20 635 088** → зеркало `тождество ±0 ✓` (`Σ 0.0206 Gram = баланс цепочки`).
- Очередь выплат: **0 ожидает, 0 failed**; сверка `confirm_broadcast_payouts` заменила
  метки `bcast:` реальными хешами.

## Блокер и решение

**Симптом.** `LiteBalancer` не поднимал ни одного пира — `have no alive peers`.
Диагностика: TCP-коннект к 2 из 4 лайтсерверов конфига проходит, но ADNL-рукопожатие
везде таймаутит (`TimeoutError`, `WinError 121`). Вывод: окружение машины режет
ADNL/TCP (файрвол/DPI пропускает только веб-порты). HTTPS-индексаторы (TonAPI,
Toncenter) работают штатно.

**Решение — HTTP-канал отправки** (коммит `2d71b17`, `app/ton_pay.py`):
- оффлайн-кошелёк казны (`provider=None`, версия v4r2/v5r1 детектится по адресу,
  пара мнемоника/адрес валидируется до сети);
- `seqno` и статус: Toncenter v3 `runGetMethod 'seqno'` (uninit → 0 → **init-external**,
  казна деплоится той же транзакцией);
- вещание: Toncenter v2 `jsonRPC sendBoc` (базовый BoC подписанного внешнего сообщения);
- единый батч-счётчик `_batch_seqno` с лайтсерверным путём (инкремент только при
  успехе — два подряд перевода не получают одинаковый seqno);
- верификация тождества: `get_public_key` контракта == производный от мнемоники
  (`e02154d2…95f6c` ✓).

Переключение прозрачное: `send_ton_transfer` при `have no alive peers`/таймауте уходит
в HTTP-канал; прочие ошибки (пара мнемоника/адрес, отказ контракта) всплывают как есть.
Тесты: `tests/test_ton_pay_http_fallback.py` (11 кейсов) + обновлён dedupe-тест
перехода в HTTP. Инфраструктура: новый `http_post_with_retry` в `app/http_utils.py`
(тот же backoff, что у GET).

## Покрытие

**Живым прогоном:** ставка в/подтверждение (watcher, tonapi), закрытие дня и расчёт
приза/рейка, деплой казны, исходящие выплаты обоих типов через HTTP, вторая сверка,
зеркало ±0, газ из цепочки.

**Только автотестами (не гонялись живыми монетами):** авто-возвраты при отмене
(`rv:`/`bank:`), возвраты при паузе, ручные `/finalize`, `/refinalize`, `/payouts`,
жизнь бота как процесса (новые дни циклом `settle_closed_rounds`), HTTP-канал для
кошелька **игрока** (ставки e2e ходят через лайтсерверы — прошли, пока сеть была жива).

## Остаток по тестнету (рекомендации)

1. Поднять бота (`python -m app.main`) на свежем контуре и дать пожить: новые дни,
   авто-закрытие, пауза/возвраты, `/payouts` вживую.
2. (Опция) HTTP-канал и для игроков — тогда e2e `stake` не зависит от лайтсерверов.
3. При смене сети без `TREASURY_WALLET_VERSION` казна v5r1 детектится автоматически —
   специальных действий нет.
# Soak-отчёт: живой кругооборот казны на тестнете

Дата: 23.09.2026 (UTC). Контур: The Way's · бот + казначей на TON testnet.

## Резюме

Полный денежный цикл протонян на **живом тестнете реальными монетами**: ставка →
подтверждение → закрытие дня → жеребьёвка → приз/рейк → отправка → сверка зеркала.
Исходящие выплаты ушли через новый **HTTP-канал отправки** (OST-блокер: ADNL/TCP до
лайтсерверов режется сетью машины), казначей задеплоен той же транзакцией.
Зеркало сходится «в ноль», очередь пуста. **668 passed, 1 skipped**, ruff чист.

## Контур

| Параметр | Значение |
|---|---|
| Сеть | testnet (global id −3) |
| Казначей | v5r1, `0:7375481e…b257ef7e` (свежий, генерирован под прогон) |
| Кошелёк игрока | v5r1 `0:1c3e8381…9f92aa` (E2E_PLAYER, id 5974934737) |
| Получатель рейка | `0:f5b1039c…b4b42` (OWNER_WALLET_ADDRESS) |
| БД | `data/e2e_testnet.db` (свежая, посеяна: Player + Round day 1 OPEN, majority, money_mode, 3 карты) |
| Ключи индексаторов | TON_API_KEY и TONCENTER_API_KEY заполнены (в `.env`) |
| Источник лайтсерверов | `LITESERVER_CONFIG_URL=https://ton.org/testnet-global.config.json` |

## Хронология денег (все суммы нанотоны, хеши hex)

| Событие | Сумма | Транзакция | memo |
|---|---|---|---|
| Ставка игрока (вход) | +500 000 000 | `a53b806e…6f46fdab` | (депозит) |
| Деплой казны (HTTP, денежно-нейтральный) | 0 | `722279eb…e7fd5c` | — |
| Приз победителю | −475 000 000 | `6131ac91…312e` | `way:1:prize#1` |
| Рейк владельцу | −2 500 000 | `13592dc4…3974` | `way:1:rake#2` |
| Авто-возврат (софак, HTTP) | −100 000 000 | `618c91ff…50cb4` | `way:1:refund#3` |

- Приз ушёл с `seqno=1`, рейк с `seqno=2` (батч-счётчик не дал коллизии),
  авто-возврат — с `seqno=3` (уже из работающего процесса, см. «Живой софак»).
- Газ/комиссии из цепочки суммарно ≈ 0.0018 Gram (деплой + 2 исходящих); возврат
  ушёл с нулевой наценкой (сумма входа 100 000 000 без вычета в пределах лимита).
- **Баланс казны 20 635 088** → зеркало `тождество ±0 ✓` (`Σ 0.0206 Gram = баланс цепочки`).
- Очередь выплат: **0 ожидает, 0 failed**; сверка `confirm_broadcast_payouts` заменила
  метки `bcast:` реальными хешами.

## Блокер и решение

**Симптом.** `LiteBalancer` не поднимал ни одного пира — `have no alive peers`.
Диагностика: TCP-коннект к 2 из 4 лайтсерверов конфига проходит, но ADNL-рукопожатие
везде таймаутит (`TimeoutError`, `WinError 121`). Вывод: окружение машины режет
ADNL/TCP (файрвол/DPI пропускает только веб-порты). HTTPS-индексаторы (TonAPI,
Toncenter) работают штатно.

**Решение — HTTP-канал отправки** (коммит `2d71b17`, `app/ton_pay.py`):
- оффлайн-кошелёк казны (`provider=None`, версия v4r2/v5r1 детектится по адресу,
  пара мнемоника/адрес валидируется до сети);
- `seqno` и статус: Toncenter v3 `runGetMethod 'seqno'` (uninit → 0 → **init-external**,
  казна деплоится той же транзакцией);
- вещание: Toncenter v2 `jsonRPC sendBoc` (базовый BoC подписанного внешнего сообщения);
- единый батч-счётчик `_batch_seqno` с лайтсерверным путём (инкремент только при
  успехе — два подряд перевода не получают одинаковый seqno);
- верификация тождества: `get_public_key` контракта == производный от мнемоники
  (`e02154d2…95f6c` ✓).

Переключение прозрачное: `send_ton_transfer` при `have no alive peers`/таймауте уходит
в HTTP-канал; прочие ошибки (пара мнемоника/адрес, отказ контракта) всплывают как есть.
Тесты: `tests/test_ton_pay_http_fallback.py` (11 кейсов) + обновлён dedupe-тест
перехода в HTTP. Инфраструктура: новый `http_post_with_retry` в `app/http_utils.py`
(тот же backoff, что у GET).

## Живой софак (headless): денежный цикл в работающем процессе

Сетевой окружение машины режет `api.telegram.org:443` (вдобавок к ADNL/TCP), поэтому
`python -m app.main` как процесс не стартует (`delete_webhook` падает). Софак прогнан
**headless**: production-джобы планировщика (`way-tick` 15с, `ton-watch` 30с,
`ton-settle` 120с, `treasury-mirror` 180с) запущены с `bot=None` на том же контуре и БД.

Сценарий: игрок через оффлайн-HTTP-перевод подкачал казну 0.1 Gram с мусорным memo
(`soak-topup`, tx `1539dbd3…3027`, player seqno=5). Дальше всё сделал работающий процесс:

1. `ton-watch` увидел вход, day 2 не открыт → итог `in:no_round` (Income #2, 100 000 000);
   повторное появление той же транзакции распознано как `refund_duplicated` (идемпотентность ✓).
2. Диспетчер: первый проход через лайтсерверы упал (`WinError 121`) → **HTTP-канал**:
   `seqno=3`, `sendBoc 200` — «Перевод 100000000 нанотонов … через HTTP (toncenter)».
3. На-цепочка: tx `618c91ff…50cb4` — исходящий 100 000 000 → `0:1c3e8381…9f92aa` ✓.
4. `confirm_broadcast_payouts`: tonapi-сверка упала `401 Unauthorized` → ушла на
   **toncenter v3 transactions** (fallback L3 ✓) — «Внесено: 1 … в БД», payout #3 `sent` с реальным хешем.
5. `treasury-mirror`: `тождество +0 (diff 0.0000 Gram)` стабильно на всех циклах после
   возврата; баланс казны не изменился (вход 100M минус выход 100M).
6. Очередь выплат 0/0; авто-возврат выбрал полную сумму входа (лимит `refund_min_gram`
   0.05 не мешает).

Алерт хранителю `_alert_http_channel_switch` в headless-режиме не вызывается
(`bot=None`, покрыт юнит-тестом), реальная доставка в TG на будущий живой запуск.

## Покрытие

**Живым прогоном:** ставка в/подтверждение (watcher, tonapi), закрытие дня и расчёт
приза/рейка, деплой казны, исходящие выплаты обоих типов через HTTP, вторая сверка,
зеркало ±0, газ из цепочки; **плюс headless-софак**: подкачка с авто-возвратом,
идемпотентность watcher, живой HTTP-dispatch (seqno=3), fallback сверки
tonapi→toncenter, зеркало ±0 после возврата.

**Только автотестами (не гонялись живыми монетами):** авто-возвраты при отмене
(`rv:`/`bank:`), возвраты при паузе, ручные `/finalize`, `/refinalize`, `/payouts`,
новые дни циклом `settle_closed_rounds` (в софаке день 2 не открылся — раунд 1 уже был
закрыт CLI-прогоном, ролик дня идёт только по событию окончания голосования),
HTTP-канал для кошелька **игрока** (`build_offline_wallet`/`send_wallet_transfer_http`,
покрыт юнит-тестами; живая ставка e2e по новому пути — следующим прогоном).

## Остаток по тестнету (рекомендации)

1. Денежный софак на боте с реальным Telegram пройден **headless** (бот-процесс не
   стартует: egress режет `api.telegram.org:443`). На машине с доступным TG —
   `python -m app.main`, свежий контур, дать пожить: новые дни (событие голосования),
   авто-закрытие, пауза/возвраты, `/payouts`.
2. HTTP-канал для игрока готов (`e2e stake` больше не зависит от лайтсерверов:
   общий `build_offline_wallet` + `send_wallet_transfer_http`; ставка идёт через
   Toncenter `runGetMethod`/`sendBoc`, неразвёрнутый контракт деплоится
   init-external'ом).
3. При смене сети без `TREASURY_WALLET_VERSION` казна v5r1 детектится автоматически —
   специальных действий нет.

## Риск: HTTP-вещание завязано на Toncenter

Вся отправка денег на этом контуре идёт через один провайдер — Toncenter
(`sendBoc`/`runGetMethod`), чтение истории — TonAPI → Toncenter. Если Toncenter
ляжет/забанчит по ключу, выплаты встанут целиком (лайтсерверы в этом окружении
недоступны по сети). Митигации: (а) `TONCENTER_API_KEY` — свой ключ (анонимный
лимит — 429 при частых выплатах), (б) алерт хранителю при переключении на HTTP-канал
уже есть, (в) при желании — второй REST-провайдер (toncenter.com/api версии в2/в3
уже используются как fallback друг к другу для чтения).